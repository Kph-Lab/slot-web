{
  "private": true,
  "devDependencies": {
    "@types/p5": "^1.3.0",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "p5": "^1.4.0"
  },
  "scripts": {
    "dev": "parcel index.html --open",
    "build": "parcel build index.html",
    "buildForCI": "parcel build index.html -d docs --public-url ."
  }
}
